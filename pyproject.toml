[project]
name = "pulsara-agent"
version = "0.1.0"
description = "Pulsara backend runtime for a JSON-LD grounded agent memory MVP."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=49.0.0",
    "httpx>=0.28.1",
    "httpx2>=2.5.0",
    "jieba>=0.42.1",
    "mcp[cli]==2.0.0",
    "openai>=2.43.0",
    "prompt-toolkit>=3.0.52",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.0",
    "pyyaml>=6.0.3",
    "cachetools>=7.1.4",
    "psycopg-pool>=3.3.1",
]

[project.scripts]
pulsara = "pulsara_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pulsara_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "postgres: integration tests that require a fresh migrated PostgreSQL database",
    "retrieval_live: opt-in tests that call live retrieval-side model APIs",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.16",
]
