# Pulsara 前端 real-LLM dogfood 目视验证报告

日期：2026-08-31
运行形态：本地 Pulsara Web 应用，`http://127.0.0.1:8765/`
真实模型：DeepSeek Chat Completions（仓库当前唯一配置的真实 provider）

## 1. 结论

本轮把仓库中的 real-LLM dogfood 逐项映射到真实前端操作，并通过浏览器实际创建会话、输入提示、切换本轮权限、启用本轮规划、停止/引导运行、触发上下文整理、展开工具和子任务卡片来目视检查结果。

- 共执行 19 个可由当前前端表达的真实模型场景。
- 19 个场景的前端投影均能正确呈现；其中 7 类显示问题是在本轮发现并修复后通过复测的。
- 16 个场景同时满足端到端行为预期。
- 3 个场景暴露了模型或运行时偏差，但前端没有掩盖它们：严格子任务调用约束偏离、完整工具结果提取漂移、第五个排队子任务激活后挂起。
- MCP、Hooks、插件/Skill 安装与语义记忆预置属于当前后端/CLI 管理面；前端没有对应入口，因此没有伪造“不可用”控件，也不把这些场景冒充为前端 real-LLM 通过。
- 当前没有配置 Responses/闭源 provider，因而只能真实验证完整 reasoning 流，不能声称已经实测 summary-only reasoning。

本报告以生产源代码与真实运行结果为主证据；根目录 README 只作为次级参考，不据其中可能过时的描述推断功能。

## 2. 状态口径

| 状态 | 含义 |
|---|---|
| 前端通过 | 浏览器中可见状态、顺序、标签、Markdown、工具或子任务投影与真实运行一致 |
| 行为偏差 | 前端忠实展示了过程，但模型没有严格满足 dogfood 的行为约束 |
| 运行时失败 | 前端正确展示排队/运行状态，但内核未能完成真实工作 |
| 无前端入口 | 场景依赖当前只存在于后端/CLI 的管理或预置能力；前端应当不展示虚假控件 |

## 3. 冻结核心套件

| 场景 | 真实前端操作与目视结果 | 结果 |
|---|---|---|
| `cache-continuity` | 连续三轮只读取一次项目说明，正确记住 `BLUE-EMBER-731`，三份输出文件包含精确标记 | 前端通过；行为通过 |
| `canonical-resume` | 读取标记后完整重启本地应用，恢复同一会话且没有再次读取，生成正确续跑结果 | 前端通过；行为通过 |
| `long-context-continuity` | 按链路顺序读取六份长上下文文件，最终恢复 `Asterford-Veylan` | 前端通过；行为通过 |
| `prompt-queue-fifo` | 第一轮执行中相隔约 180 ms 提交第二条输入；前端同时显示两条用户消息与“1 条输入正在等待处理”，结果严格先后写入 | 前端通过；行为通过 |
| `subagent-delegation` | 子任务在主时间线中完整可见，含思考、工具、结果、`1 / 1 已完成`，最终算出 `367:2043` | 前端通过；严格行为偏差：父任务多做了一次终端查看，子任务多搜索一次 |
| `workspace-patch` | 浏览器中观察到基线 2 个失败、修改过程、最终 2 个通过，并生成精确 `PATCH_NOTES` | 前端通过；行为通过 |

## 4. 专项场景

### 4.1 规划、权限、停止与引导

| 场景 | 真实前端操作与目视结果 | 结果 |
|---|---|---|
| 本轮规划 | 点击输入框旁的“先规划”，完成问题选择、Markdown 方案渲染、方案修订、批准执行；取消规划后下一轮不残留 | 前端通过；行为通过 |
| 本轮权限拒绝 | 在工具权限确认中拒绝终端操作；卡片最终显示“已拒绝 / 操作未完成”，不再误报完成 | 前端通过；行为通过 |
| 用户停止 | 停止 20 秒前台命令；卡片显示“已取消 / 操作已取消”，下一轮能读到类型化结果 | 前端通过；行为通过 |
| 运行中引导 | 在 8 秒前台命令执行时引导当前任务；消息在安全点被吸收，最终输出 `STEER-UI-ACCEPTED` | 前端通过；行为通过 |

### 4.2 上下文整理与工具结果

| 场景 | 真实前端操作与目视结果 | 结果 |
|---|---|---|
| 空闲时整理上下文 | 触发整理，前端显示进行状态；可回收时整理后继续对话并保留连续性。对已经较紧凑的真实会话复测时，显示绿色“无需整理上下文 / 当前上下文已经较紧凑，本次整理无法进一步缩小”，不显示失败 | 前端通过；行为通过 |
| 执行中整理上下文 | 前台 8 秒工具运行时点击整理；界面同时显示“进行中”和“正在整理上下文”。本次无进一步回收空间时出现与空闲态逐字相同的绿色提示，随后工具完成且主 Agent 继续给出自然语言 `ACTIVE_COMPACTION_UI_OK_2`，没有停在工具或压缩操作上 | 前端通过；行为通过 |
| 完整工具结果 | 34,029 字节结果保持为折叠摘要，没有把工具正文倾倒到卡片外 | 前端通过；模型提取偏差：模型把标记后的大量填充字符复制进最终回答 |
| 头尾投影 | 62,053 字节结果只提供头尾窗口，模型得到精确 `ROUND71_HEAD` 与 `ROUND71_TAIL`，未读取保留内容 | 前端通过；行为通过 |
| 保留内容读取 | 模型通过两页保留内容找到 `ROUND71_MIDDLE_SENTINEL_73`；工具标签为“读取保留内容” | 前端通过；行为通过 |

### 4.3 子任务编排

| 场景 | 真实前端操作与目视结果 | 结果 |
|---|---|---|
| 六任务依赖图 | 六张子任务卡片均可见，依赖任务按序完成，汇总显示 `6 / 6 已完成`，六个摘要均匹配预期 | 前端通过；行为通过 |
| 四并发 + 一个排队 | 创建五个独立子任务后，前四个显示“进行中”，第五个立即显示“待开始”，不会等到启动后才突然出现 | 前端通过；运行时失败，见第 6 节 |
| 最近一轮上下文 + 运行中消息 | 主任务运行中连续引导两条 marker，子任务采用最近一轮上下文；随后通过真实前端向执行中的子任务发送一条消息。卡片从 `0 / 1` 变为 `1 / 1 已完成`，摘要准确列出两条父上下文 marker 与一条 mailbox 消息 | 前端通过；行为通过 |
| 显式带入子任务结果 | 前端真实创建 `envelope_worker`，子任务只通过结果工具提交 `PROVIDER_ENVELOPE_DOGFOOD_A7C1`；主任务先结束并等待人工操作。随后在会话检查器点击“带入会话并继续”，时间线只显示中性事件，主模型经统一 compiler 的 typed user-role 信封返回“已收到 PROVIDER_ENVELOPE_DOGFOOD_A7C1” | 前端通过；行为通过 |

最后一个场景的真实观察值为：

- `ROOT_CONTEXT_MARKER_ONE_7C1A`
- `ROOT_CONTEXT_MARKER_TWO_9B4E`
- `MIDFLIGHT_GUIDANCE_MARKER_2D6F`
- 工具边界输出 `WORKER_BOUNDARY_DONE`

## 5. 所有 real-LLM dogfood 的前端覆盖映射

| 仓库入口 | 前端对应覆盖 | 判定 |
|---|---|---|
| 冻结 core v1 六场景 | 第 3 节六项 | 已真实执行 |
| `run_round4_plan_dogfood.py` | 本轮规划、问题、Markdown 方案、修订、批准、取消 | 已真实执行 |
| `run_round5a2_durable_replay_dogfood.py` | 应用重启后的 canonical resume | 已在当前 Chat Completions provider 执行；Responses provider 未配置 |
| `run_round5b_compaction_dogfood.py` | 空闲与执行中上下文整理 | 已真实执行 |
| `run_round7_failure_observation_dogfood.py` | 用户停止；另补充权限拒绝与运行中引导 | 已真实执行 |
| `run_round7_1_tool_result_projection_dogfood.py` | 完整、头尾、保留内容三种投影 | 已真实执行 |
| `run_round10_subagent_dogfood.py` | 依赖图、最近一轮上下文/消息、容量排队 | 已真实执行前端可表达部分；容量场景发现运行时失败 |
| `run_round9_capability_dogfood.py` | MCP 配置/reload 是后端管理面；前端只投影真实出现的工具，不提供虚假管理控件 | 无直接前端入口 |
| `run_round9_2_hook_dogfood.py` | Hook 来源与信任管理尚无前端入口 | 无直接前端入口 |
| `run_round9_3_plugin_dogfood.py` | 插件安装、启停、检查、回收尚无前端入口 | 无直接前端入口 |
| `run_round9_3_plugin_installer_discovery_dogfood.py` | 安装器发现依赖 CLI/Skill 指南 | 无直接前端入口 |
| `run_unified_skill_dogfood.py` | bundled/loose Skill 激活与覆盖尚无前端入口 | 无直接前端入口 |
| `evals/recall/real_llm_dogfood.py` | 依赖先在后端预置 durable memory 与搜索索引，前端当前没有记忆管理/预置入口 | 无直接前端入口 |

设置页也进行了实际目视检查：只展示当前真实存在的外观、DeepSeek 服务和本地运行信息；登录区域明确为“无需账号”；没有出现“不可用”“尚未实现”“即将推出”，也没有 MCP、Hooks、插件、Skill 等缺少内核接线的占位控件。权限只位于输入框附近并绑定单轮，规划也是输入区的单独按钮。

## 6. 本轮发现但未伪装成前端问题的运行时缺陷

容量场景中，前四个子任务完成后，第五个任务从 `PENDING` 转为 `ACTIVE`，但数分钟内没有任何模型输出；主任务等待与停止也无法完成。停止本地服务时，内核报告 `subagent cancellation winner conflicts`。

前端在这个过程中正确展示了：

1. 五个任务从创建时起全部可见；
2. 前四个“进行中”、第五个“待开始”；
3. 第五个获得容量后转为进行中；
4. 重启后，孤立的历史子任务和未闭合工具不会继续伪装成“进行中”，而是显示中性的“已结束 / 操作未完成”。

因此该项判定为“前端容量投影通过、内核运行时完成失败”。本轮没有为了让前端变绿而吞掉或伪造这个失败。

## 7. 前端修正

本轮针对真实 dogfood 轨迹完成以下修正：

1. 精确识别用户拒绝工具执行的结果，卡片显示“已拒绝 / 操作未完成”。
2. `artifact_read` 使用面向用户的“读取保留内容”，不再显示泛化的“使用工具”。
3. 从 canonical task 列表预先建立全部子任务投影，让受容量限制的任务在启动前就显示“待开始”。
4. 历史记录中只有 transcript、没有终态的孤立子任务显示“已结束”，不误报为仍在运行。
5. 不属于当前活动轮次的未闭合工具轨迹显示“已结束 / 操作未完成”，避免重启后出现永远旋转的工具卡片。
6. 已接纳的子任务结果显示为中性的“已带入子任务结果”，不再伪装成“你”，不泄漏结果 ID、工具 acknowledgement 或原始 JSON；悬停/聚焦提供产品说明。
7. live snapshot 中已经脱离 canonical active control 的残留 ROOT/child draft 不再宣称会话仍在运行；真实 dogfood 中数据库已完成但页面持续“进行中”的状态已修复并目视复测。

## 8. 回归验证

| 验证 | 结果 |
|---|---|
| 前端定向回归 | 新增 stale child live-draft 门禁通过；active ROOT reasoning 与已完成 child 投影两项邻接门禁通过 |
| `npm run lint` | 通过 |
| `npx tsc --noEmit` | 通过 |
| `npm run build:local` | 通过；本地嵌入静态资源成功生成 |
| provider/result 定向回归 | 3 个门禁全部通过：explicit result acceptance、typed lowering envelope、continuity contract |
| real-LLM 浏览器复测 | 真实 child 创建/完成、人工接纳、typed provider delivery、自然语言回复、中性 UI 事件与空闲态收尾全部通过 |

最终结论：当前前端已经能够忠实、可视地承载现有会话、规划、权限、工具、停止/引导、上下文整理和子任务编排的 real-LLM 轨迹；后端没有提供产品入口的能力不会以“不可用”占位暴露。仍需单独修复的唯一阻断性问题是子任务容量释放后的第五个任务可能挂起。
