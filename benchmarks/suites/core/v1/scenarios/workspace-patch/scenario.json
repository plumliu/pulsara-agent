{
  "schema_version": "pulsara.kernel-dogfood-scenario.v2",
  "scenario_id": "workspace-patch",
  "description": "Fix a bounded retry queue through the canonical Kernel tool loop and satisfy a hidden behavioral verifier.",
  "model_role": "pro",
  "timeout_seconds": 480,
  "system_prompt": "You are running a frozen Pulsara engineering dogfood. Work only in the current project. Inspect relevant source and visible tests, make the smallest correct patch, and run visible tests. Never inspect credentials. A hidden verifier exists outside the workspace.",
  "fixture": {
    "fixture_kind": "directory_copy",
    "workdir": "workdir"
  },
  "workflow": {
    "workflow_kind": "workspace_task",
    "prompt": "Fix retry_queue.py. drain_with_retries must preserve input order, call worker at most max_retries + 1 times per item, stop retrying an item after success, and return one Result per input item. Invalid negative max_retries must raise ValueError. Keep the public API unchanged, run the visible tests, and write PATCH_NOTES.md containing the exact line RETRY_QUEUE_FIXED_V1 when finished."
  },
  "verifier": {
    "path": "verify.py",
    "timeout_seconds": 30
  },
  "evidence_gate": {
    "min_root_turns": 1,
    "max_root_turns": 1,
    "min_all_turns": 1,
    "max_all_turns": 1,
    "min_model_calls": 1,
    "max_model_calls": 12,
    "min_tool_calls": 2,
    "max_tool_calls": 32,
    "committed_event_minimums": [
      {"event_type": "AssistantMessageAccepted", "minimum": 1},
      {"event_type": "ToolResultAccepted", "minimum": 2},
      {"event_type": "TurnCompleted", "minimum": 1}
    ],
    "forbidden_event_types": ["TurnInterrupted"],
    "expected_writer_generation_delta": 0
  }
}
