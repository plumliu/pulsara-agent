{
  "schema_version": "pulsara.kernel-dogfood-scenario.v2",
  "scenario_id": "cache-continuity",
  "description": "Exercise three canonical turns, exact prefix continuity, and provider-reported process-local cache usage.",
  "model_role": "flash",
  "timeout_seconds": 600,
  "system_prompt": "You are running Pulsara's Kernel cache-continuity dogfood. Use workspace tools exactly as requested. Preserve facts learned in prior turns and do not reread files when forbidden. Never inspect credentials or environment variables. Keep responses concise.",
  "fixture": {
    "fixture_kind": "directory_copy",
    "workdir": "workdir"
  },
  "workflow": {
    "workflow_kind": "cache_continuity",
    "prompts": [
      "Read project_notes.md once. Find the authoritative project codename, then write cache_round1.txt whose entire contents are CODENAME|phase-one using that exact codename. Do not modify project_notes.md.",
      "Do not read or search any file in this turn. Using only the prior conversation, write cache_round2.txt whose entire contents are the same authoritative CODENAME followed by |phase-two.",
      "Do not read or search any file in this turn. Using only the retained conversation, write cache_final.txt whose entire contents are the same authoritative CODENAME followed by |phase-three. Use write_file exactly once."
    ],
    "inter_turn_delay_seconds": 1.0
  },
  "verifier": {
    "path": "verify.py",
    "timeout_seconds": 20
  },
  "evidence_gate": {
    "min_root_turns": 3,
    "max_root_turns": 3,
    "min_all_turns": 3,
    "max_all_turns": 3,
    "min_model_calls": 3,
    "max_model_calls": 10,
    "min_tool_calls": 4,
    "max_tool_calls": 16,
    "committed_event_minimums": [
      {"event_type": "AssistantMessageAccepted", "minimum": 3},
      {"event_type": "ToolResultAccepted", "minimum": 4},
      {"event_type": "TurnCompleted", "minimum": 3}
    ],
    "forbidden_event_types": ["TurnInterrupted"],
    "root_turn_tool_gate": {
      "turn_selector": "last",
      "required_exact_counts": [
        {"tool_name": "write_file", "exact_count": 1}
      ],
      "forbidden_tool_names": ["read_file", "search_files", "terminal"]
    },
    "require_positive_cached_input_tokens": true,
    "expected_writer_generation_delta": 0
  }
}
